# Brief: How to Get a Table of Contents When You Convert Markdown to Word

**Cluster:** Use Cases & Reference (cluster-3-post-1)
**Primary keyword:** markdown to word table of contents
**Secondary keywords:** auto generate toc in word from markdown, word table of contents not updating, heading styles for table of contents
**Template:** how-to
**Target word count:** ~1,200
**URL:** `/blog/markdown-to-word-table-of-contents`

## Meta Description
Word can generate a table of contents automatically — but only if your headings converted as real Heading styles, not bold text. Here's how to check and fix it.

## Outline
- H1: How to Get a Table of Contents When You Convert Markdown to Word
- H2: Word's table of contents needs Heading styles, not bold text
- H2: How to check if your converted document qualifies (View → Navigation Pane)
- H2: Inserting an automatic TOC (References → Table of Contents)
- H2: Why the TOC won't update on its own after edits, and how to refresh it
- H2: What to do if your headings didn't convert as real styles

## Key points to cover
- Make the dependency explicit upfront: an automatic TOC in Word is built entirely from Heading style levels, so this post only works if the conversion step produced real styles — directly link back to the headings-not-converting post for readers whose document fails this check.
- Walk through Word's native References → Table of Contents feature precisely (which built-in style most people miss) since many readers may not know it exists at all, separate from any Markdown-specific concern.
- Cover the "why didn't my TOC update" follow-up question explicitly (Word TOCs are static snapshots that need a manual refresh, not live fields) since this is a common point of confusion after the TOC is first inserted.

## Internal links to include
- → Pillar `/markdown-to-word` — anchor: "markdown to word converter" (mandatory)
- → `/blog/markdown-resume-to-word` — anchor: "markdown resume to word" (in-cluster, recommended)
- → `/blog/markdown-cheatsheet` — anchor: "markdown cheatsheet" (in-cluster, recommended)
- → `/blog/markdown-to-word-headings-not-converting` — anchor: "heading levels and Word styles" (cross-cluster, optional)

## Competing pages to differentiate from
- Generic "how to add a table of contents in Word" guides assume you're typing directly in Word and never address the Markdown-conversion prerequisite.
- Converter-tool FAQ pages mention TOC support as a checkbox feature without explaining the underlying heading-style dependency.
